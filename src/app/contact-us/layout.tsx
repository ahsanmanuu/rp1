export const metadata = {
  title: "Contact Us | Latexify",
  description: "Get in touch with Latexify. We'd love to hear from you.",
};

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
