# Plan: Fix ESLint Warnings in 4 Files

## File 1: `src/components/scholarly-editor/ScholarlyPDFViewer.tsx`

**Identified warnings:**
1. `PDFJS_VERSION` (line 8) — declared but never used
2. `onSync` (line 182) — destructured prop but never referenced in component body
3. Possibly `React` default import — only used in type positions (`React.MouseEvent`, `React.CSSProperties`), may be flagged if config ignores type-only usage
4. Will verify 4th with ESLint after fixes

**Fixes:**
- Remove `const PDFJS_VERSION = '5.6.205';` (line 8)
- Remove `onSync` from the destructured props on line 182
- Run ESLint to verify and catch any remaining

---

## File 2: `src/app/admin/emails/page.tsx`

**Identified warnings:**
1. `React` default import (line 3) — unused with automatic JSX transform; no `React.` value references in this file
2. `adminNotifications` state (line 58) — setter called on line 89 but value never read in JSX
3. `showToast` function (line 128) — defined but never called anywhere
4. `<img>` on line 181 — `@next/next/no-img-element` ESLint rule

**Fixes:**
- Change `import React, { useState, useEffect }` → `import { useState, useEffect }`
- Remove `adminNotifications` state declaration and `setAdminNotifications(...)` call on line 89
- Remove `showToast` function definition (lines 128-131)
- Replace `<img src="/logo.png" ...>` with `<Image>` from `next/image`

---

## File 3: `src/app/admin/offers/page.tsx`

**Identified warnings:**
1. `<img>` on line 469 (logo in sidebar) — `@next/next/no-img-element`
2. `<img>` on line 917 (chat attachment) — `@next/next/no-img-element`
3. `loadingChatSessions` state (line 43) — setter called on lines 71/88 but value never read in JSX
4. Will verify 4th with ESLint after fixes

**Fixes:**
- Replace `<img src="/logo.png" ...>` with `<Image>` from `next/image`
- Replace `<img src={m.attachmentUrl} ...>` with `<Image>` from `next/image`
- Remove `loadingChatSessions` state and its setter calls (`setLoadingChatSessions(true/false)`)
- Run ESLint to verify

---

## File 4: `src/components/scholarly-editor/Doc2Latex/DocToolbar.tsx`

**Identified warnings:**
1. `Plus` import from lucide-react (line 6) — not used in JSX
2. `Edit2` import from lucide-react (line 6) — not used in JSX
3. `createNewFile` destructured prop (line 43) — never referenced in component body
4. `projectId` destructured prop (line 52) — never referenced in component body

**Fixes:**
- Remove `Plus` and `Edit2` from the lucide-react import
- Remove `createNewFile` and `projectId` from the destructured props
- Keep them in the interface (they may be used by parent components or future code)

---

## Execution Order

1. Fix ScholarlyPDFViewer.tsx
2. Fix admin/emails/page.tsx
3. Fix admin/offers/page.tsx
4. Fix DocToolbar.tsx
5. Run ESLint on all 4 files to verify 0 warnings remain
6. Fix any additional warnings surfaced by ESLint
